
export class FiltroCandidato {
  estudando: boolean;
  programando: boolean;
  trabalhando: boolean;

  constructor(estudando: boolean, programando: boolean, trabalhando: boolean) {
    this.estudando = estudando;
    this.programando = programando;
    this.trabalhando = trabalhando;
  }
}
